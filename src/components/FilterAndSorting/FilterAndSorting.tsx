import { Box } from '@mui/material'
import React from 'react'

interface IFilterAndSortingProps {}

export const FilterAndSorting = (props: IFilterAndSortingProps) => {
  return (
    <Box sx={{ height: '50rem', width: '20rem', backgroundColor: '#f4f4f4'}}>
    </Box>
  )
}